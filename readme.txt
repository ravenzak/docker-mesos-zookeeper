
1. run command:
# export: DOCKER_IP=127.0.0.1
2. run command:
# docker-compose up (stack will start in terminal tray) or run with daemon:
# docker-compose up -d
To check services run:
#  docker-compose ps
127.0.0.1:5050 - mesos master
127.0.0.1:8080 - marathon

